# ===========================================
# Meteora LP Monitor - 环境变量示例
# ===========================================
# 复制此文件为 .env 并填入实际值

# Solana 网络配置
SOLANA_NETWORK=devnet
# SOLANA_NETWORK=mainnet-beta
# SOLANA_NETWORK=testnet

# Solana RPC URL (可选，如果不设置将使用默认RPC)
# SOLANA_RPC_URL=https://api.devnet.solana.com
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_RPC_URL=https://your-custom-rpc-url.com

# Solana 提交确认级别
SOLANA_COMMITMENT=confirmed

# 调试和测试配置
ENABLE_DEBUG_LOGS=true
ENABLE_MOCK_DATA=false
NODE_ENV=development

# 应用配置
PORT=3000

# 数据库配置 (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=meteora_lp_devnet
DB_USER=meteora_user
DB_PASSWORD=your_secure_password_here

# Redis配置 (缓存)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here

# API配置
# BirdEye API Key (用于价格数据)
BIRDEYE_API_KEY=your_birdeye_api_key_here

# 安全配置
ENCRYPTION_KEY=your-256-bit-secret-key-here-change-this-in-production

# 交易配置
DEFAULT_SLIPPAGE=1.0
MAX_SLIPPAGE=5.0

# CORS配置
CORS_ORIGIN=http://localhost:8080

# 缓存配置
CACHE_TTL=300
PRICE_CACHE_TTL=60

# ========================================
# 重要安全提醒：
# ========================================
# 1. 复制此文件为 .env 
# 2. 填入实际的配置值
# 3. 确保 .env 在 .gitignore 中
# 4. 不要提交包含敏感信息的配置文件
# 5. 定期轮换密钥和凭证
EOF < /dev/null
